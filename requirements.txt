# requirements.txt — AuraRAG v3.6
#
# Changes v3.6:
#   BUG-UNSTRUCTURED fix: removed `unstructured` — it is never imported in the
#   codebase and pulls in a ~2 GB dependency tree (detectron2, tesseract
#   bindings, etc.) that causes pip timeouts on Hugging Face Spaces and bloats
#   the Docker image significantly.
#
# Changes v3.5 / v3.4:
#   CPU-only torch installed first via the PyTorch index URL in Dockerfile.
#   Added langgraph, langgraph-checkpoint for the agentic pipeline.

# ── LangGraph — agentic pipeline (v3.4+) ─────────────────────────────────────
langgraph>=0.2.0
langgraph-checkpoint>=1.0.0

# ── LangChain — core + community + provider integrations ─────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-google-genai>=1.0.0
langchain-ollama>=0.1.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Embeddings + re-ranking ───────────────────────────────────────────────────
# CPU-only torch should be installed first in the Dockerfile via the PyTorch
# index URL to avoid pulling the GPU variant (several GB).
sentence-transformers>=3.0.0

# ── BM25 ──────────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Document loaders ──────────────────────────────────────────────────────────
pypdf>=4.0.0
# NOTE: `unstructured` intentionally removed — not used anywhere in the codebase.
# BUG-W fix: autodetect_encoding in TextLoader requires python-magic
python-magic>=0.4.27

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
requests>=2.32.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Session persistence (optional — activate via REDIS_URL env var) ───────────
redis>=5.0.0

# ── Structured ingestion / UI rendering ──────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.0
pyarrow>=16.0.0
mistune>=3.0.0
