# requirements.txt — AuraRAG v4.0
#
# Changes v4.0:
#   Added pandas>=2.0.0            — dataframe ingestion (CSV, JSON, XLSX, Parquet)
#   Added openpyxl>=3.1.0          — pandas XLSX engine
#   Added pyarrow>=14.0.0          — pandas Parquet engine
#   Added fastparquet>=2023.10.1   — fallback Parquet engine
#
# Retained from v3.4:
#   CPU-only torch installed first in Dockerfile via the PyTorch index URL.
#   langgraph + langgraph-checkpoint for the agentic pipeline.
#   All provider LangChain integrations.

# ── LangGraph — agentic pipeline ─────────────────────────────────────────────
langgraph>=0.2.0
langgraph-checkpoint>=1.0.0

# ── LangChain — core + community + provider integrations ─────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-google-genai>=1.0.0
langchain-ollama>=0.1.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Embeddings + re-ranking ───────────────────────────────────────────────────
# CPU-only torch must be installed FIRST in Dockerfile via PyTorch index URL.
sentence-transformers>=3.0.0

# ── BM25 ──────────────────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Document loaders — core formats ──────────────────────────────────────────
pypdf>=4.0.0
unstructured>=0.14.0
python-magic>=0.4.27        # TextLoader autodetect_encoding (BUG-W fix)

# ── Dataset / structured file support (v4.0) ─────────────────────────────────
pandas>=2.0.0               # CSV, JSON, XLSX, Parquet dataframe ingestion
openpyxl>=3.1.0             # pandas XLSX read engine
pyarrow>=14.0.0             # pandas Parquet read engine (preferred)
fastparquet>=2023.10.1      # Parquet fallback engine

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
requests>=2.32.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Session persistence (optional — activate via REDIS_URL env var) ───────────
redis>=5.0.0
